NODE_ENV=development

# 应用名，用于显示pm2服务等场景
APP_NAME=nodebbs

# 主机/端口
HOST=0.0.0.0
PORT=7100

# 数据库
DATABASE_URL=postgres://postgres@0.0.0.0:5432/nodebbs

# 缓存配置
REDIS_URL=redis://default:password@redis:6379/0

# 用户信息缓存配置（秒）
# 用于缓存用户角色、封禁状态等信息，减少数据库查询
# 建议值：开发环境 30-60，生产环境 120-300
USER_CACHE_TTL=30

# JWT 使用 `openssl rand -base64 32` 生成
JWT_SECRET=change-this-to-a-secure-random-string-in-production
JWT_ACCESS_TOKEN_EXPIRES_IN=1y

CORS_ORIGIN=*

# 前端 URL，用于发送验证邮件和构建 Oauth 回调 URL
APP_URL=http://localhost:3100

# COOKIE 配置(备用，通常不用设置)
# COOKIE_SECRET=change-this-to-a-secure-random-string-in-production
# COOKIE_SECURE=true
# COOKIE_SAMESITE=Lax
# COOKIE_DOMAIN=.example.com